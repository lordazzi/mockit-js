import { ValidMockFileService } from './valid-mock-file.service';

describe('Testing ValidMockFileService', () => {
    const storage: ValidMockFileService = ValidMockFileService.getInstance();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
