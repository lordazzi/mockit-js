import { StreamIOService } from './stream-io.service';

describe('Testing StreamIOService', () => {
    const storage: StreamIOService = StreamIOService.getInstance();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
