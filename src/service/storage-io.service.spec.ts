import { StorageIOService } from './storage-io.service';

describe('Testing StorageIOService', () => {
    const storage: StorageIOService = StorageIOService.getInstance();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
