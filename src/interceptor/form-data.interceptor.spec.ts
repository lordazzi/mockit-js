import { FormDataInterceptor } from './form-data.interceptor';

describe('Testing FormDataInterceptor', () => {
    const storage: FormDataInterceptor = new FormDataInterceptor();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
