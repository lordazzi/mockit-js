import { RequestSpyInterceptor } from './request-spy.interceptor';

describe('Testing RequestSpyInterceptor', () => {
    const storage: RequestSpyInterceptor = new RequestSpyInterceptor();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
