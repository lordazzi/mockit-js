import { ArgumentInterceptor } from './argument.interceptor';

describe('Testing ArgumentInterceptor', () => {
    // const storage: ArgumentInterceptor = new ArgumentInterceptor();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
