import { FakeRequestInterceptor } from './fake-request.interceptor';

describe('Testing FakeRequestInterceptor', () => {
    const storage: FakeRequestInterceptor = new FakeRequestInterceptor();

    // it('Sum', () => {
    //     expect(calc.sum(1, 1)).toBe(2);
    // });
});
